import { ParamValidationPipe } from './param-validation.pipe';

describe('ParamValidationPipe', () => {
  it('should be defined', () => {
    expect(new ParamValidationPipe()).toBeDefined();
  });
});
